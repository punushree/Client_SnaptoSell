/**
 * My Detections Page
 * Displays all product detections for the logged-in user
 */

import type { FC } from "react";
import UserDetections from "@/components/UserDetections";

const Page: FC = () => {
  return <UserDetections />;
};

export default Page;

